/*
CSCI 104: Homework 1 Problem 1

Use this file to test your split.cpp file.

Create linked lists and split them with your split() function.

You can compile this file like this:
g++ split.cpp test_split.cpp -o test_split
*/

#include "split.h"
#include <cstddef>
#include <iostream>
using namespace std;
int main(int argc, char* argv[])
{
  /*Has valgrind errors but the testing portions worked
  commented out since no longer needed
   Node* element1 = new Node(1,NULL);
   Node* element2 = new Node(2,NULL);
   Node* element3 = new Node(3,NULL);
   Node* element4 = new Node(4,NULL);
   Node* element5 = new Node(5,NULL);
   element1->next=element2;
   element2->next=element3;
   element3->next=element4;
   element4->next=element5;
   Node* evens = NULL;
   Node* odds = NULL;
   split(element1,odds,evens);
   std::cout << odds->value << "\n";
   std::cout << evens->value << "\n";
   std::cout << odds->next->value << "\n";
    delete element1;
    delete element2;
    delete element3;
    delete element4;
    delete odds;
  */

    
}
